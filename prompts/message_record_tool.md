Record the structured interpretation of the message. USE exactly once. Intent catalogue: salary_increase (regular pay raised to X from date), salary_reduced (next salary reduced to X, e.g. unpaid leave), salary_temporary_pay (temporary monthly pay X continues), salary_date_moved (confirmed salary now expected on DATE), first_salary_confirmed (first salary X on DATE from a new employer), salary_resumes (regular salary X resumes on DATE), employment_ended, seasonal_contract_ended, household_income_ended (one household income ended; remaining monthly salary X), bonus_unapproved, commission_unapproved, arrears_one_time (regular salary X plus a one-time arrears Y: report X only), invoice_confirmed (client approved invoice X settling on DATE; others pending), gig_payout_pending (platform payout pending / not withdrawable), rent_increase (multiplier e.g. 1.12), self_transfer, refund_pending, foreign_refund_pending, foreign_charge_pending, foreign_salary_confirmed (salary X in a foreign currency confirmed for DATE), prize_pending, prize_settled, investment_value_moved, investment_sale_settled, reimbursement_not_salary, duplicate_charge_dispute_open, duplicate_charge_reversed, failed_debit_retry, two_card_minimums, receipt_reference (points to a receipt image), scam_or_instruction, other. Fill every field (use null where not applicable). If the message contradicts itself or cannot be tied to this user's finances, call declare_unresolvable instead.
