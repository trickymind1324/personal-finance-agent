Record the final structured reading of the image. USE exactly once, after inspecting the image. amount = the figure the linked event should carry (null only if nothing readable); amount_kind = which printed line; partial = true if cropped or no grand total visible; evidence_text = the verbatim line. If the image is unreadable or unrelated to the event, call declare_unresolvable instead.
